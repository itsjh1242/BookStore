<style>
    .bestSeller{ color: orange; }
    .inline-block-center {
        display: inline-block;
        margin-left: 40px;
        margin-right: 40px;
        margin-bottom: 30px;
    }
    .wrapper{
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            margin-top: 2rem;
    }
</style>
<div class="wrapper">
    <div class="alert alert-primary" role="alert" style="width: 960px; min-width: 960px;">
        Dongseo Univ, 20181495, Kim Jun Hyeon <br>| Node Js를 이용한 도서구매 사이트 Beta |<br>✨<a href="https://github.com/itsjh1242/Node.js/tree/main/book_store">Github BookStore Beta</a>
    </div>
</div>
<div class="container-fluid">
    <br>
    <div class = "container-sm text-center">
        <h1 class = "bestSeller"> 🎊Best Seller🎊 </h1>
    </div>
    <br>
    <div class="container text-center">
        <% for(var i = 0; i < 3; i++) { %>
            <div class="card inline-block-center" style="width: 18rem;">
                <img src="../images/book/book<%= rows[i].book_num %>.jpeg" class="card-img-top" alt="..." height = "400" width = "150">
                <div class="card-body">
                    <h5 class="card-title"><%= rows[i].book_title %><span class="badge bg-secondary"><%= i + 1 %>위</span></h5>
                    <h6 class = "card-text"> 가격: 
                        <% if(rows[i].book_price.toString().length === 5) {%>
                            <%= rows[i].book_price.toString().substring(0,2) %>,<%= rows[i].book_price.toString().substring(2) %>
                        <% }%>
                        원
                    </h6>
                    <p class="card-text">판매량: <%= rows[i].book_salesRate %></p>
                    <form method="post">
                        <input type="hidden" name="bookinfo" id="bookinfo" value="<%= rows[i].book_index %>">
                        <input type="submit" value = "상세보기" class="btn btn-primary">
                    </form>
                </div>
            </div>
        <% }; %>
    </div>
    <br>
</div>
